# Docker image for Google SDK & Node

Docker image to run gcloud and node commands (with bower and gulp installed globaly)

This image is available on [docker hub!](https://hub.docker.com/r/amalheiros/node-gcloud/)